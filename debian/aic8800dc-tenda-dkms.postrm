#!/bin/sh
set -e

if [ "$1" = remove ] || [ "$1" = purge ]; then
    depmod -a || true
    if command -v udevadm >/dev/null 2>&1; then
        udevadm control --reload || true
    fi
fi

#DEBHELPER#

exit 0
